# Allow mismatched peer deps from packages that haven't updated their
# peer dep declarations for React 19 yet (e.g. @react-three/drei@9 lists
# @react-three/fiber@^8 but fiber v9 is fully compatible at runtime).
legacy-peer-deps=true
